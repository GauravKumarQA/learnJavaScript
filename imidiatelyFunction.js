"use strict ";

//When we want function which will be only executed once So we use imidiatedly called function
//The function doesnot have any name.
//The function is wrap with () and to call it immidiately we use () at the end

(function () {
  console.log("I am an imidiately execute function");
})();
